VERSION/
├── MASTER.blend
├── README.md
├── VALIDATION.md
├── BASE-SPEC.json             # only when a base or mounting interface exists
├── BASE-REPORT.json           # only when a base or mounting interface exists
├── JOB-RECORD.json            # long, paid, or multi-session jobs only
├── SHA256SUMS
├── inputs/
├── exports/
├── renders/
├── bambu-studio-delivery/     # candidate/final 3MF when Bambu delivery is requested
├── audits/                    # color/geometry/import audits when generated
└── reusable-parts/            # only when reusable independent parts exist
